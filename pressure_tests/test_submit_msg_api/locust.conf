# Locust 配置文件
# 在这里设置默认的压力测试参数

# Web UI 监听地址
host = http://localhost:7860

# 默认并发用户数
users = 10

# 用户孵化速率（每秒新增用户数）
spawn-rate = 2

# 运行时长（可选，例如: 60s, 5m, 1h）
# run-time = 5m

# 是否开启 Web UI（headless 模式设为 false）
# headless = false

# 日志级别
loglevel = INFO
