# 医院内网版重构提交记录

本文件按 Git 提交逐项记录本地化重构的改造动机、范围和注意事项。Git
提交信息统一使用英文，本文件使用中文说明，便于医院信息科、运维和后续开发者
追溯变更。

## `fix: harden chat browser interactions`

- **改造动机**：引用联动脚本会把证据段落的 `textContent` 重新赋给
  `innerHTML`，不仅丢失原始结构，还可能把已经转义的文档文本重新解释为 HTML；
  脚本同时存在立即执行的错误定时器、未声明全局变量以及缺少空节点保护等问题。
- **前端修复**：证据定位改为只遍历和拆分文本节点，并仅清理本功能创建的高亮标记，
  不再重建证据 HTML 或破坏引用标记；修正全文检索延迟调用、中文思维导图提示、
  空节点防护和局部变量作用域。输入状态脚本不再主动改写 Gradio 的 `disabled`，避免
  模型生成期间意外重新启用发送按钮造成重复请求。
- **隐私与冗余**：SSO 登录、会话可见性和推理管线日志不再打印用户对象、用户 ID、
  URL 列表或完整推理状态，只保留不含业务正文的结构化诊断信息；删除未被事件链使用
  的 `ChatPanel.submit_msg` 旧回调，实际提交仍统一走 `ChatPage.submit_msg`。
- **验证范围**：增加前端证据高亮安全回归，执行 JavaScript 语法、Ruff 和聊天安全、
  汉化、通知模块测试；后续如升级 Gradio，应继续验证其生成期间按钮禁用语义。

## `feat: add vision image chat input`

- **改造动机**：聊天区虽然使用 Gradio 多模态输入组件，但全局样式隐藏了原生上传
  按钮，提交回调也完全忽略 `files` 字段，导致已配置的 `qwen3-vl-flash` 只能收到
  文本，无法使用视觉能力。
- **输入交互**：恢复并重绘输入框左下角图片按钮，使用与参考界面一致的加号和分隔线；
  输入框保留右侧麦克风/发送状态切换，添加图片后自动切换为可发送状态。上传范围
  收敛为 PNG、JPEG、WebP，并在消息气泡中显示附件名称。
- **模型链路**：提交阶段保留清洗后的问题和临时图片路径，推理前验证格式、大小、
  像素及数量，再编码为 OpenAI-compatible `image_url` 数据内容。用户图片不依赖文档
  证据的多模态开关，始终优先于知识库图片传入 VL 模型；不把 Base64 写入会话数据库。
- **启动与安全**：LLM 启动检查改用程序生成的 64×64 纯白图片验证视觉请求，而非只
  测试文本。默认限制每次 4 张、单张 8 MB、2500 万像素，避免伪装文件、损坏图片和
  超大解码请求；医院外部模型档下仍需遵守患者图片脱敏及出网审批。

## `fix: suppress known dependency startup warnings`

- **改造动机**：LanceDB 的可选 ColPali 模块与当前 Pydantic 命名空间规则冲突，旧版
  pypdf 又会触发 Cryptography 的 ARC4 迁移提示。两条信息均在导入阶段出现，不影响
  当前 Chroma/LanceDB、PDF 读取或模型调用，却容易被运维人员误认为启动失败。
- **改动范围**：仅在应用配置入口按完整警告文本过滤这两条已知依赖提示，不屏蔽其他
  `UserWarning`、弃用警告或运行异常；新增独立进程测试确认应用和 pypdf 导入时不再
  输出对应内容。
- **注意事项**：Lance 首次创建空数据集的日志仍保留，因为它反映真实的索引初始化
  行为；依赖升级后应删除已不需要的精确过滤规则，而不是扩大过滤范围。

## `fix: validate bootstrap users before model checks`

- **改造动机**：模型预检会先创建应用数据库中的模型配置表，旧静态自检只判断
  `sql.db` 文件是否存在，因而把“数据库存在但用户表为空”误认为已有管理员。启动
  脚本随后先完成三次模型调用，直到构建管理页面才报告缺少首次管理员密码。
- **用户判断**：静态自检改为只读查询用户表，至少存在一个用户才允许移除启动密码；
  空数据库仍要求显式管理员账号和至少 12 位密码。医院部署同时校验用户管理已启用，
  SSO 场景则不要求本地启动密码，保持现有认证路径兼容。
- **启动顺序**：`start`、`restart`、`foreground` 和 Docker 前台入口先执行静态
  `doctor`，通过后才启动可选本地 Rerank 并调用四类模型。配置错误会在产生模型调用
  费用前返回非零状态，页面和本地重排进程均不会启动。
- **注意事项**：不恢复旧 `dev` 分支不安全的 `admin/admin` 默认值，也不会自动生成
  运维人员未知的登录凭据。现有 `.env` 首次迁移时需补充
  `KH_FEATURE_USER_MANAGEMENT_ADMIN` 和 `KH_FEATURE_USER_MANAGEMENT_PASSWORD`。

## `docs: align the hospital feature overview`

- **改造动机**：中文首页仍声称医院版保留 MCP，但管理员入口、运行配置和医院部署
  档已按既定要求移除或关闭 MCP，文档描述会误导部署和验收人员。
- **改动范围**：从项目能力概览中移除 MCP，仅保留实际可用的文档问答、PaddleOCR、
  医疗中文界面、共享知识库、模型配置和语音助手描述；不修改任何运行代码或配置。

## `fix: prevent sensitive model logging`

- **改造动机**：问答管线的调试输出直接打印完整 Retriever 和改写管线对象。模型
  对象的字符串表示包含 API Key，问题改写结果还可能包含患者描述，医院日志因此
  存在凭据和诊疗信息泄露风险。
- **改动范围**：调试日志只保留改写管线类型、改写完成状态和 Retriever 数量，不再
  序列化模型对象，也不记录原问题或改写后的文本。检索数量等不含业务正文的诊断
  信息保持不变，方便定位检索链路问题。
- **注意事项**：历史日志不会被自动删除，升级时应按院内密钥轮换和日志留存制度
  处理已有日志；本次改动不改变模型调用、检索结果或回答逻辑。

## `feat: gate startup on model service checks`

- **改造动机**：原启动脚本只负责按需拉起本地重排服务，无法在页面启动前确认当前
  默认 LLM、Embedding、Rerank 和 ASR 是否可用；密钥、地址或模型名错误只能等医生
  首次提问或上传文档后才暴露。应用后台启动还只检查进程存活两秒，页面尚未就绪或
  随后退出时也会误报成功。
- **模型预检**：新增统一启动预检，依次向默认 LLM 发起最小问答、生成单条向量并
  校验维度、对两条短文本执行重排序并校验分数。任一必需服务失败时返回非零状态，
  不启动页面；异常输出会隐藏 API Key 和 Authorization。ASR 关闭或使用 Mock 时
  明确显示 `[SKIP]`，未来真实 Provider 必须实现健康检查后才能通过。
- **启动可靠性**：`start`、`restart`、`foreground` 以及医院 Docker 入口使用同一
  预检链路；后台启动显式传递 Gradio 端口，并等待首页 HTTP 可访问后才报告成功。
  进程提前退出、超时或非法超时配置均返回失败并清理 PID，默认等待 90 秒，可通过
  `KH_APP_START_TIMEOUT` 调整。
- **注意事项**：`doctor` 保持为不调用模型 API 的静态环境检查；启动预检会产生极少
  量模型调用费用。可选本地 Rerank 仍先按配置启动，再进入与远程模型一致的实际请求
  校验，预检失败时会停止本次临时拉起的重排进程。

## `feat: add traceable user notifications`

- **改造动机**：原聊天链路只捕获 `ValueError` 并打印到终端。模型连接失败、超时
  或其他异常会直接中断页面，医生只能看到笼统的 Error，运维也无法将页面报错与
  服务端日志对应。
- **改动范围**：新增统一通知模块，为非预期异常生成短故障编号；页面展示经过
  脱敏的中文原因和处理建议，服务端日志保留故障编号及完整堆栈。新增滚动日志
  配置，默认写入应用数据目录的 `logs/app.log`。
- **聊天容错**：主回答失败时保留已生成内容并附加错误说明；会话自动命名和推荐
  追问改为失败后降级，不再影响已经生成的答案；空问题改为明确中文提示。
- **注意事项**：故障编号只用于关联日志，不包含用户、病历或模型密钥信息。可用
  `KH_LOG_LEVEL`、`KH_LOG_MAX_BYTES` 和 `KH_LOG_BACKUP_COUNT` 调整日志级别与
  保留策略。

## `feat: localize and refine the clinical interface`

- **改造动机**：医生端和管理员端存在中英文混排，部分按钮、表头、内容类型和
  模型连接状态含义不明确；聊天区、文件区与证据区的视觉层级也不够稳定。
- **改动范围**：增加轻量中文文案表，在不改变选项内部值的前提下翻译动态设置；
  统一聊天、文件/分组、索引、LLM、Embedding、Rerank、帮助页等核心界面的
  中文按钮、表头、状态和操作提示。
- **界面调整**：增加独立医疗场景样式文件，改善左右侧栏、聊天气泡、输入区、
  文件表格和移动端布局。样式复用现有 Gradio 主题变量，不引入新的前端框架。
- **兼容说明**：模型名称、供应商类名、YAML 参数名等技术标识保留原文；翻译后的
  下拉选项只改变显示标签，不改变数据库和推理管线使用的值。

## `fix: harden credentials and document rendering`

- **改造动机**：原系统将用户名和密码同时写入浏览器 `localStorage`，密码使用无盐
  SHA-256 保存；上传文档中的原始 HTML 还可能进入证据面板，存在凭据泄露和存储型
  XSS 风险。
- **密码改造**：浏览器只记住用户名，不再保存或自动回填密码；新建和修改的密码
  使用带独立随机盐、60 万次迭代的 PBKDF2-SHA256。历史 SHA-256 密码仍可验证，
  并在用户首次成功登录后自动升级，不需要统一停机迁移。
- **内容安全**：文档正文进入 Markdown 渲染前先转义原始 HTML，文件名、页码预览
  属性、图片标题和高亮文本分别按 HTML 文本或属性规则转义，阻止文档脚本在其他
  用户浏览器执行。
- **注意事项**：本提交不改变现有用户名和用户 ID，也不会在日志中记录明文密码；
  SSO 创建的无本地密码账号仍只能通过 SSO 登录。

## `feat: add hospital deployment profiles`

- **改造动机**：医院环境不能依赖公网 CDN、GitHub、NLTK/tiktoken 临时下载或
  Chroma 遥测；数据库中历史公共 Provider 即使不再显示，也会在启动时被模型
  Manager 反序列化并可能联网。
- **部署档位**：新增 `hospital-external` 和 `hospital-offline`。前者仅允许白名单
  HTTPS 模型域名和内网地址，后者仅允许回环、私网 IP、单标签服务名及
  `.local/.internal/.lan` 地址。医院档强制关闭 MCP、网页抓取、外部 Agent、远程
  帮助、Gradio API 展示和遥测。
- **资源本地化**：固定并内置 Tribute、MiniSearch、D3、Markmap 依赖；PDF 预览
  改用随包 PDF.js，首次运行只解压本地归档；内置 LlamaIndex 所需 NLTK 英文数据，
  同时提供不下载词表的本地 Token 估算器。
- **模型白名单**：医院模式只加载当前配置档声明的 LLM、Embedding 和 Rerank。
  旧公共 Provider 记录保留在数据库但不实例化；管理员新增模型时同样执行地址
  策略校验，避免通过自定义 Base URL 绕过出口限制。
- **配置模板**：新增 `.env.hospital.example`，分别给出 GeekAI 过渡档和最终内网
  OpenAI-compatible 模型网关配置。真实密钥和管理员密码不得提交到 Git。

## `fix: harden file indexing and archive uploads`

- **改造动机**：上传索引的总异常会直接把底层异常展示给医生，单文件失败数量还
  错误地统计了上传前校验结果；快速上传失败时可能继续拼接空返回值，引发二次异常。
- **错误反馈**：索引总异常和逐文件异常统一生成故障编号，页面仅展示中文说明，
  完整堆栈写入滚动日志；完成提示按索引管线的真实失败结果统计。快速上传在失败时
  返回空结果，不再遮盖首次故障。
- **压缩包安全**：每次上传使用独立临时目录，避免并发用户互相删除解压文件；拒绝
  路径穿越、绝对路径和符号链接，并限制文件数量及解压后总大小，处理结束自动清理。
- **配置项**：`KH_ZIP_MAX_FILES` 控制单个压缩包最多文件数，
  `KH_ZIP_MAX_SIZE_MB` 控制解压后总大小；默认分别为 200 个和 500 MB。

## `fix: improve database and indexing reliability`

- **改造动机**：SQLite 默认锁等待较短，多名医生同时保存会话、设置或索引时容易
  出现 `database is locked`；快速上传又会把向量写入放到无返回通道的线程，页面
  可能在 Embedding 实际失败前显示成功。
- **数据库加固**：SQLite 连接启用 30 秒忙等待、跨线程连接、外键约束、WAL 日志
  和 `NORMAL` 同步级别，在保持单文件部署方式的同时改善读写并发。非 SQLite
  数据库不改变现有连接行为，可用 `KH_SQLITE_BUSY_TIMEOUT_MS` 调整等待时间。
- **索引一致性**：医院部署档的快速上传仍使用默认解析器，但 Embedding 与向量库
  写入改为请求内同步执行；失败会沿现有故障编号链路返回页面，不再出现后台静默
  失败或“先成功、后缺向量”的状态。
- **数据模型**：JSON 字段改用独立默认工厂，避免会话、用户设置、问题报告和索引
  配置在进程内意外共享同一个可变默认对象。

## `feat: add hospital deployment preflight checks`

- **改造动机**：迁移到另一套内网环境时，密钥占位符、弱启动密码、错误模型地址、
  公网功能开关或缺失静态资源通常要等应用启动失败后才能发现，定位成本较高。
- **自检范围**：新增不访问模型服务的启动前检查，验证部署档位、模型配置、地址
  白名单、首次管理员密码、公共 Provider 遗留密钥、外部功能开关、离线前端/PDF/
  分词资源、数据目录写权限和剩余磁盘空间。
- **运维入口**：`./run.sh doctor` 输出逐项 `[PASS]`、`[WARN]`、`[FAIL]` 和汇总，
  失败时返回非零状态，便于部署脚本或 systemd 在启动前阻断错误配置；输出不会打印
  任何密钥或密码内容。
- **兼容说明**：已有用户数据库时允许不再保留启动管理员明文密码，但会提示确认
  管理员账号可用；ASR 开启但没有服务地址时仅警告当前将使用 Mock Provider。

## `feat: add one-command hospital deployment`

- **改造动机**：直接在医院服务器运行 `uv sync` 或 Docker 在线构建仍会访问软件源，
  且容易因机器环境差异产生不可复现的问题；手工启动也缺少固定的数据挂载、健康
  检查和回滚入口。
- **镜像交付**：新增医院专用多阶段 Dockerfile，在联网构建机固定 Python 和 uv
  版本并安装文档解析运行库；最终镜像内已包含 Python 依赖、PDF.js、NLTK 和浏览器
  资源，运行阶段不下载依赖。构建上下文明确排除真实 `.env` 和应用数据。
- **一键启动**：新增 Docker Compose 和 `deploy_hospital.sh`。配置完成后，一条命令
  会先执行医院自检，再以 `--pull never` 启动固定镜像；也保留已准备好虚拟环境时的
  原生部署方式。数据统一持久化到项目根目录 `ktem_app_data`。
- **安全与运维**：容器使用宿主当前 UID/GID、丢弃 Linux capabilities、禁止权限
  提升并内置健康检查。新增中文手册说明联网构建/内网运行边界、镜像校验、模型
  切换、故障编号、备份、升级、回滚和上线验收清单。

## `fix: disable framework network checks`

- **改造动机**：完全断网启动回归发现，虽然业务功能和静态资源均已离线化，Gradio
  仍会在后台线程获取官方提示文案；Hugging Face/Transformers 组件在缓存缺失时也
  可能尝试下载模型或数据集。
- **改动范围**：医院模式在任何 Gradio 页面构建前关闭 Analytics/在线文案更新，
  同时启用 Hugging Face Hub、Datasets 和 Transformers 离线开关；Docker 运行镜像
  也显式固定相同环境变量，避免入口脚本差异绕过设置。
- **验证结果**：使用 Socket 拦截器构建完整 UI（423 个组件、247 条依赖）时，模型
  池仅保留 GeekAI 配置、MCP 关闭、页面 Head 不含公网 URL，后台网络连接尝试为 0。

## `chore: remove public provider defaults`

- **改造动机**：通用 `.env.example` 仍沿用上游模板，列出了 OpenAI、Azure、
  Cohere、Voyage、GraphRAG、Adobe 和 Google 登录等公共服务。医院模式虽不会加载
  这些配置，但实施人员复制模板时仍可能误配或误以为系统需要通用互联网出口。
- **配置收敛**：通用模板改为医院档，默认只声明 GeekAI 过渡模型、最终内网模型
  网关示例、可选内网 Keycloak、离线资源和运行容量参数；删除所有无关公共 Provider
  密钥字段与公网地址。上游 Provider 适配器源码不删除，便于后续 rebase，但医院
  模式的 Manager 白名单仍确保它们不会实例化。
- **文档入口**：中文首页改为复制 `.env.hospital.example`，要求启动前运行
  `./run.sh doctor`，并链接医院离线构建、备份和回滚手册。

## `feat: add realtime asr service foundation`

- **改造动机**：原语音助手依赖独立 iframe 和固定服务地址，既割裂医生的问答
  流程，也无法统一管理说话人分离、声纹识别和会话数据。医院内网部署还需要明确
  区分可验收的模拟链路与尚未接入的真实 ASR，避免将演示结果误用于临床。
- **服务边界**：新增 Provider 无关的转写事件、分段、说话人和声纹数据结构，提供
  可重复测试的 Mock Provider、安全 HTML 渲染、管理员权限校验及 SQLite 持久化。
  Mock 会明确标记“模拟数据”；真实音频流和内网 ASR 协议需在接口确定后新增适配器。
- **数据与权限**：新增独立的 ASR 配置表和声纹元数据表，沿用应用数据库并自动建表；
  配置及声纹写操作复用现有管理员校验，普通用户不能读取接口密钥或维护声纹。

## `feat: add asr administration controls`

- **改造动机**：语音识别需要像语言模型一样独立配置，且旧语音 iframe 与医院内网
  一体化使用方式不符；管理员页面中的 MCP 入口也不属于当前医院部署范围。
- **管理页面**：资源管理新增“语音识别模型”和“声纹库”页签，前者明确展示当前仅
  可用的模拟供应商及预留的模型、地址、密钥和超时字段，后者支持管理员注册、查看
  和二次确认删除声纹。启用用户管理时两个页签只向管理员显示。
- **模块收敛**：移除资源管理中的 MCP 页签、系统设置中的 MCP 动态工具刷新以及旧
  语音助手 iframe 页面；医院构建固定关闭 MCP，真实内网 ASR 可用前继续显示模拟
  标识，并允许通过 `KH_ENABLE_ASR=false` 关闭入口。
- **配置文档**：更新环境变量模板、中文首页和迁移说明，记录 ASR 的配置边界、应用
  构建烟测及本地测试现状；配置模板不包含任何真实接口密钥。

## `feat: integrate clinical chat workflows`

- **改造动机**：语音结果原先位于独立页面，医生无法在同一会话中回看；用户问题也
  缺少可靠的修改、重新生成和删除链路，操作失败时容易出现会话与证据状态错位。
- **语音会话**：聊天页新增 ASR 开始、流式更新、取消和持久化事件链，转写内容作为
  AI 侧消息写入当前会话，并与证据、图表占位保持索引对齐；模型推理、会话命名和
  后续问题生成会过滤仅用于展示的 ASR 消息，避免把转写卡片 HTML 发送给 LLM。
- **消息修改**：修改用户问题时从目标轮次截断后续消息、证据和图表，再复用现有问答
  管线重新生成；删除操作校验会话所有权，并同步删除对应证据、图表及调整后续点赞
  索引，防止跨用户或错位修改。
- **异常处理**：ASR Provider 异常会转换为对话内中文失败状态，取消录音保留已完成
  分段；转写 HTML 对供应商文本进行转义，声纹维护和模型配置均覆盖权限测试。

## `feat: redesign clinical chat interactions`

- **输入区**：将发送与语音合并为输入框右下角的单一操作位；无文字时显示线性麦克风
  图标，输入文字或附件后自动切换为圆形发送箭头，录音时显示可再次点击取消的红色
  动态波形。隐藏 Gradio 自带的重复提交按钮，保持键盘提交能力不变。
- **消息操作**：为每条用户消息增加仅在悬浮或聚焦时出现的复制、修改、删除图标。
  修改状态在原消息位置展示文本框及“取消/发送”，支持 Escape 取消和
  Command/Ctrl+Enter 发送；语音消息位于历史前方时仍按真实会话索引操作。
- **语音样式**：多说话人转写卡片直接显示在 AI 回复区域，区分说话人颜色、时间、
  临时/最终文本和声纹置信度；限制卡片宽度与纵向高度，窄聊天区不再出现横向滚动。
- **兼容性**：不依赖 Gradio 是否为用户消息生成原生操作栏，而是注入独立操作栏并
  通过隐藏的 Gradio 桥接组件调用服务端权限回调，避免框架版本差异导致图标消失。

## `fix: complete Chinese administration UI`

- **改造动机**：系统设置和模型管理仍直接展示上游英文参数名、内部 Provider 类名
  及英文默认提示词，医生难以判断参数用途，也容易将兼容接口误解为公网 OpenAI。
- **设置汉化**：补齐解析器、召回数量、检索模式、MMR、重排序、相关性评分、推理
  方式、引用、思维导图和提示词等可见术语；选择项只翻译显示标签，保存值仍保持
  `simple`、`hybrid` 等内部标识，保证已有配置兼容。
- **模型管理**：语言模型、嵌入模型、重排序模型和索引列表改用中文供应商/类型名称，
  默认状态显示“是/否”，修复语言模型默认项说明中残留的英文拼接；实际 Provider
  类名和数据库规格不变。
- **默认提示词**：医院版常规问答、复杂问题分解及系统提示词改为中文，并保留
  `{context}`、`{question}`、`{lang}` 模板变量和“不确定时不得编造”的约束。
  新增测试确认中文标签不会改变内部值，聊天编辑和 ASR 展示不会进入 LLM 历史。

## `feat: refine clinical chat workspace`

- **改造动机**：桌面端会话列表与参考证据同时展开时，1280 像素视口中的主聊天区
  仅剩约 505 像素；移动端仍保留较大的 Gradio 默认边距。医生的主要操作被次要信息
  挤压，空白会话也缺少图片、知识库引用及临床使用边界提示。
- **工作区布局**：参考证据改为按需打开，并保持按钮主次状态与面板可见性一致；桌面端
  保留会话列表，960 像素以下默认折叠，两个面板的入口都固定在聊天画布上。消息流与
  输入区统一限制为 980 像素并居中，减少大屏阅读跨度；移动端收紧页面留白，避免横向
  滚动，并允许随时重新展开会话列表。
- **交互与视觉**：输入区增加聚焦反馈、圆角和轻量阴影，保留“空输入显示语音、输入
  内容显示发送”的单操作位；AI 消息的框架复制按钮改为悬浮显示，用户消息仍使用已有
  的复制、修改、删除操作。新会话以中文卡片说明图片输入、知识库引用及“仅供临床
  参考”的边界。
- **兼容与验证**：未引入新前端框架或依赖，继续复用 Gradio 的事件、状态、鉴权和流式
  输出链路。新增参考证据默认状态及切换单元测试，并在 1280×720 与 390×844 视口完成
  浏览器回归，确认侧栏切换、证据切换、发送/语音切换和无横向溢出。

## `style: clear static analysis findings`

- **改造动机**：全仓 Ruff 检查仍报告 4 项问题，其中 3 项来自必须在可选依赖导入前
  执行的告警过滤器，另 1 项是模型预检测试中的局部 lambda 赋值。虽然不影响运行，
  但会让持续集成无法把新的静态问题与已知设计区分开。
- **改动范围**：对告警过滤器之后的延迟导入增加精确的 `E402` 说明，并保持过滤顺序
  不变；将测试中的 Embedding lambda 改为等价的局部函数。未改变模型配置、启动顺序
  或预检断言。
- **验证结果**：全仓 `ruff check .` 通过，模型服务预检测试保持通过。

## `docs: record current architecture review`

- **改造动机**：已有迁移文档侧重从 `dev` 到官方 v0.12 的历史选择，缺少当前运行
  链路、代码风险、UI 决策和 Gradio/Vue 技术选型的统一说明；其中 Agent/MCP 段落
  还停留在早期“保留管理入口”的状态，与医院版当前实现不一致。
- **架构评审**：新增当前启动、入库、检索、四类模型、会话及 ASR 数据流说明，逐项
  记录本轮已修复问题、上游依赖告警、孤立测试、Mock ASR 和 Gradio DOM 适配等边界；
  明确公共 Provider 核心源码保留与医院运行时不可达是两个不同层次。
- **前端决策**：对比 Gradio 与 Vue 的复用成本、部署复杂度、UI 上限和医院回归风险，
  当前选择继续优化 Gradio；给出需要重新评审 Vue 的触发条件及“先 API、再管理页、
  最后聊天页”的渐进迁移路径。
- **文档校准**：修正迁移说明中的 MCP/Agent 医院策略和最新测试数字，并记录应用层、
  核心库、静态检查、真实模型预检、HTTP 启动及桌面/移动端浏览器验证基线。

## `fix: stabilize document retrieval add-ons`

- **改造动机**：真实 PDF 检索会将首轮扩展得到的 100 个长片段一次性发送到 GeekAI
  重排序接口；供应商在请求体过大时可能返回 HTTP 200 但结果为空，旧适配器把它作为
  致命异常，导致单文件后续追问和“全部文件”引用都只显示故障编号。原有启动检查仅用
  两条短文本，无法覆盖这一故障形态。
- **重排序稳定性**：GeekAI 适配器按文档数量和总字符数分批请求，对单个超长片段只在
  重排请求中截断，最终仍返回完整原文；各批结果按相关度全局合并，并保留原始文档身份。
  接口超时、空结果或结构异常时降级为原始检索顺序并写入告警及降级元数据，避免可选的
  排序增强阻断整个知识库问答。
- **思维导图**：引用与思维导图使用独立超时，思维导图等待时间由原来的固定 10 秒延长
  为默认 30 秒；可选生成线程改为守护线程并单独捕获异常。修复浏览器后处理脚本中的
  未定义变量，确保导图导出事件和引用链接统计不会在回答完成后触发 JavaScript 异常。
- **验证范围**：新增长候选集分批、请求字符上限、空结果降级、未知返回文档降级及思维
  导图独立超时测试；同时保留现有 GeekAI 文档映射、多模态消息和嵌入批处理测试。
